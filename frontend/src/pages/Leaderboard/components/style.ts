import { styled } from '@mui/material/styles';

export const LeaderboardPaper = styled('div')(() => ({
    backgroundColor: '#F2F7FD',
    borderRadius: '71px',
    minHeight: '1100px',
    height: 'fit-content',
}))
